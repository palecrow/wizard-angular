describe('hz.app.dashboard module tests:', function() {

  beforeEach(module('hz.app.dashboard'));

  it('should define module "hz.app.dashboard".', function () {
    expect(angular.module('hz.app.dashboard')).toBeDefined();
  });

});