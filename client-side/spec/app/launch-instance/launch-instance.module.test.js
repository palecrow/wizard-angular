describe('hz.app.launch-instance module tests:', function() {

  beforeEach(module('hz.app.launch-instance'));

  it('should define module "hz.app.launch-instance".', function () {
    expect(angular.module('hz.app.launch-instance')).toBeDefined();
  });

});